#generate.gcs
#Exécuter la ligne de commande (voir plus bas la section exec)
command val exec
#Insérer la variable *code dans l'input avec l'id 4. (*code est une variable créée par le programme code)
SetInputText 4 *code
end

[exec]
code create
[/exec]